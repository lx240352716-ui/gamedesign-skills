# Game Documents

Place `.docx` and `.xlsx` design documents in this directory.

After running `python references/scripts/core/doc_reader.py`, parsed markdown will be cached in `.cache/`.

Source files (.docx/.xlsx) are not tracked by git.
